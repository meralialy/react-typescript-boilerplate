services:
    web:
        build:
            context: .
            target: development
        ports:
            - "${PORT:-3000}:${PORT:-3000}"
        volumes:
            - .:/app
            - /app/node_modules
